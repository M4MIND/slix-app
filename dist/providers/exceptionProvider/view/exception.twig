<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <style>
        * {
            box-sizing: border-box;
        }

        html, body {
            margin: 0;
            padding: 0;
            font-family: "Helvetica Neue", "Lucida Grande", "Lucida Sans Unicode", Helvetica, Arial, sans-serif;
            font-size: 14px;
            background-color: #f5f5f5;
            color: #252525;
        }

        .exception {

        }

        .exception-head {
            background: #3b579d;
            min-height: 200px;
            display: flex;
        }

        .exception-head-error-block {
            width: 100%;
            max-width: 1000px;
            padding: 2rem 0 6rem 0;
            margin: auto;
            color: #FFF;
            font-size: 1.75rem;
        }

        .exception-body {
            max-width: 1000px;
            padding: 2rem;
            background-color: #FFFFFF;
            margin: -4rem auto 0;
            border-radius: .4rem;
            box-shadow: 0 4px 20px 1px rgba(0, 0, 0, .06), 0 1px 4px rgba(0, 0, 0, .08)
        }

        .budget {
            display: inline-block;
            background-color: rgba(0, 0, 0, .05);
            padding: 0.4rem 1rem;
            border-radius: 0.4rem;
            font-size: 1rem;
        }

        h3 {
            color: #444444;
            text-transform: uppercase;
        }

        .hr {
            border-bottom: 1px solid #eee;
            display: block;
        }

        pre {
            overflow: auto;
            background-color: rgba(0, 0, 0, .04);
            padding: 1rem;
            border-radius: 0.4rem;
            border: 1px solid rgba(0, 0, 0, .1);
            margin-bottom: 0;
        }
    </style>
</head>
<body>
<div class="exception">
    <div class="exception-head">
        <div class="exception-head-error-block">
            {{ exception.message }}
        </div>
    </div>
    <div class="exception-body">
        <h3>Code: <span class="budget">{{ exception.code }}</span></h3>
        <div class="hr"></div>
        <pre>{{ exception.stack }}</pre>
    </div>
</div>
</body>
</html>